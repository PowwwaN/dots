# Example Theme Configuration
# This file demonstrates all available theme configuration options for Vicinae.
# You don't need to define all of these - only override what you want to customize.
# Undefined colors will be automatically derived from core colors or inherited from base themes.
#
# Where a color name is expected, you can use the following hex formats:
# #RGB
# #RRGGBB
# #AARRGGBB (with alpha)
# 'transparent' (no color)
# <color_ref> (e.g 'colors.accents.red'): references this theme's definition if available, the parent's otherwise
#
# Where a color is expected, you can either use a simple string for the color name or the following table syntax:
# [colors.core]
# accent = "accents.color.red"
# OR:
# accent = { name = "accents.color.red", opacity = 0.5 }
# OR:
# accent = { name = "accents.color.red", lighter = 100 }
# OR:
# accent = { name = "accents.color.red", darker = 150 }

[meta]
name = "Oxocarbon"                         # Display name shown in theme picker
description = "Oxocarbon palette"  # Description shown in theme picker
variant = "dark"                              # Theme variant: "dark" or "light". Every dark theme inherits from vicinae-dark and every light theme from vicinae-light. 
inherits = "vicinae-dark"                     # Optional: inherit from another theme (e.g., "vicinae-dark")
icon = "icons/theme-name.png"                 # Optional: path to theme icon (relative to the theme directory it is placed in if not an absolute path)

# ============================================================================
# Core Colors
# ============================================================================
# These are the most important colors that define your theme's overall look.
# Most other colors can be automatically derived from these if not specified.

[colors.core]
accent = "#ff4297"              # Primary accent color (used for input borders, text selection background...)
accent_foreground = "#636262"   # Text color on accent-colored backgrounds
background = "#161616"          # Main application background
foreground = "#f2f4f8"          # Primary text color
secondary_background = "#393939" # Background for surfaces elevated above main (popups, tooltips, panels)
border = "#393939"              # Default border color throughout the UI

# ============================================================================
# Window-Specific Colors
# ============================================================================
# Override border colors for specific window types

[colors.main_window]
border = "#393939"

[colors.settings_window]
border = "#393939"

# ============================================================================
# Accent Colors
# ============================================================================
# Your color palette, central to your theme's identity

[colors.accents]
blue = "#00c15a"
green = "#00b4ff"
magenta = "#c693ff"
orange = "#ff4297"
red = "#00dfdb"
yellow = "#ff4297"
cyan = "#ff74b8"
purple = "#c693ff"

# ============================================================================
# Text Colors
# ============================================================================
# Colors for various text states and meanings

[colors.text]
default = "#f2f4f8"             # Usually same as core.foreground
muted = "#636262"               # Secondary text: hints, less important info
danger = "#00dfdb"              # Errors or warnings
success = "#00b4ff"             # Success or positive states
placeholder = "#f2f4f8"			# input placeholder, search placeholder...
selection = { background = "#00c15a", foreground = "#f2f4f8" }

[colors.text.links]
default = "#00c15a"
visited = "#c693ff"

# ============================================================================
# Input Fields
# ============================================================================
# Colors for text inputs, search boxes, etc.

[colors.input]
border = "#393939"
border_focus = "#00b4ff"
border_error = "#00dfdb"

# ============================================================================
# Buttons
# ============================================================================
# Colors for button states

[colors.button.primary]
background = "#393939"
foreground = "#f2f4f8"
hover = { background = "#00dfdb2e" }
focus = { outline = "colors.core.accent" }

# ============================================================================
# Lists
# ============================================================================
# Colors for list items (horizontal list views)

[colors.list.item.hover]
background = "#393939"
foreground = "#f2f4f8"

[colors.list.item.selection]
background = "#393939"          # Selected item on main background
foreground = "#f2f4f8"
secondary_background = "#585858" # Selected item on secondary surfaces (popups, panels)
                                 # Light themes: lighter than primary selection
                                 # Dark themes: lighter/brighter than primary selection
secondary_foreground = "#f2f4f8"

# ============================================================================
# Grid Items
# ============================================================================
# Colors for grid-based layouts (icon grids, tiles, etc.)

[colors.grid.item]
background = "#393939"
hover = { outline = "#f2f4f8" }
selection = { outline = "#f2f4f8" }

# ============================================================================
# Scrollbars
# ============================================================================

[colors.scrollbars]
background = "#393939"

# ============================================================================
# Loading Indicators
# ============================================================================

[colors.loading]
bar = "#f2f4f8"     # shown right below the search bar when something is loading
spinner = "#f2f4f8" # used by dynamic toasts

